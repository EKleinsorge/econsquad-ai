# Temporary book extraction file (regenerate with: node supabase/build-book-sql.js)
book-extract.txt

# Supabase local temp files
supabase/.temp/

# Build/scratch artefacts that should never be committed.
# deploy-log.txt is rewritten by deploy-webhook.bat on every run.
deploy-log.txt
index-backup-*.html

# One-off scratch: the body fragment security.html was assembled from.
_security_body.html

# Claude's working folder: delivered SQL, .bat scripts and the screenshots
# taken while testing. Kept on disk deliberately as a record of what was
# handed over, but it is not repo content and it made `git status` unusable
# by burying real untracked files - which is how a deployed edge function
# and an applied security migration sat untracked for nine days.
Claude outputs/
